/* import { SignUp } from '../../src/components/Register.js';
import { onNavigate } from '../../src/main.js';
import { routes } from '../../src/components/Router.js'; */

jest.mock('../../src/Firebase/Firebase-util.js');

// Intento fallido main Line-16 routes not function
/* describe('Función onNavigate', () => {
  it('La función onNavigate debe cargar vista register', () => {
    document.body.innerHTML = '<div id="root"></div>';
    const  registerDiv = SignUp();
    console.log('uno', register);
    const routess = routes;
    console.log('dos', routess);
    expect(onNavigate('/Register')).toEqual(registerDiv);
  });
}); */

// Intento Fallido

/* const jsdom = require('jsdom');

const { JSDOM } = jsdom;
console.log(JSDOM); */

/* const rootDiv = new JSDOM('<!DOCTYPE html><div id="root"></div>');
    console.log(rootDiv.window.document.querySelector('#root'));
describe('Register', () => {
  it('Dar el click', () => { */
/* const dom = new JSDOM('<!DOCTYPE html><div id="root"></div>');
    console.log(dom.window.document.querySelector('#root')); */
/*   const Div = document.body.innerHTML('<div id="root"></div>');
   console.log(Div);
    const components = {
      register: () => {
        const divLogin = document.createElement('div');
        divLogin.innerHTLM = '';
        return divLogin;
      },
    }; */
/*    console.log('dos', rootDiv);
    const result = SignUp();
    const pass = result.querySelector('#register-password');
    const passConf = result.querySelector('#confirm-password');
    console.log('que hace pass', components);
  });
}); */
